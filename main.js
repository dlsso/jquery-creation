$(document).on('ready', function() {
  
  	// Loop over each 'li'
	$('li').each(function(index, domElement){

		// Append some exclamation points to the li
		// that is current in the loop
		$(this).append('!!')



	})



});